apiVersion: v2
name: longhorn
version: 0.1.0
dependencies:
  - name: longhorn
    # Verify against https://charts.longhorn.io before bumping.
    version: 1.12.0
    repository: https://charts.longhorn.io
