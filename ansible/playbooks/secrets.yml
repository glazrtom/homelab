# Optional: reseal application secrets without redeploying anything.
- name: Reseal application secrets
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - cloudflare
    - secrets
